import { TaskBase } from './models/task-base';

export class TaskTextbox extends TaskBase<string> {
  controlType = 'textbox';
  type: string;

  constructor(options: {} = {}) {
    super(options);
    this.type = options['type'] || '';
  }
}
